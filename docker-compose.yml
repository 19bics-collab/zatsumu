services:
  app:
    build: .
    restart: unless-stopped
    environment:
      ZATSUMU_RETENTION_DAYS: ${ZATSUMU_RETENTION_DAYS:-30}
    volumes:
      - app-data:/data

  caddy:
    image: caddy:2
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      # .env で ZATSUMU_DOMAIN=kintai.example.com を設定すると
      # Let's Encrypt の証明書を自動取得して HTTPS で公開する
      ZATSUMU_DOMAIN: ${ZATSUMU_DOMAIN:-localhost}
      # メール対応の画面を出すサブドメイン（勤怠とは別UI・同じサーバ/DB）。
      # DNS は ZATSUMU_DOMAIN と同じ IP に向ける
      ZATSUMU_MAIL_DOMAIN: ${ZATSUMU_MAIL_DOMAIN:-mail.localhost}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config

volumes:
  app-data:
  caddy-data:
  caddy-config:
